<!-- TOMBSTONE [9k5h.9]: this file previously held a versioned capture of
     https://agentskills.io/specification (Snapshot ID 2026-05-07-initial). That
     copy diverged from the 2026-05-28 capture in intent-eval-lab and is retired —
     the lab vendored tier + the kernel are the single source for upstream spec
     content. -->

# AgentSkills.io Open Spec Snapshot — moved to the lab vendored single source

This vendored copy (2026-05-07 capture) is retired. Consult instead:

1. **Lab vendored single source** — `intent-eval-lab` `specs/_vendor/` (watcher-fed, surface `agentskills-spec`) and `_vendor/upstream/skill-frontmatter/agentskills-spec-v1.0.md` (prose snapshot, fetched 2026-05-28, drift-watched).
2. **Kernel upstream-base projection** — `@intentsolutions/core` `schemas/authoring/v1/upstream-base/skill-frontmatter.v1.json` (machine-readable; what validators compose from). IS marketplace policy lives in the kernel `is-overlay/`, not in upstream captures.
3. **Live upstream** — <https://agentskills.io/specification>.

The IS-authored interpretation docs (`6767-b-SPEC-DR-STND-claude-skills-standard.md`, `SCHEMA_CHANGELOG.md`) remain — they are synthesis, not upstream duplication.
